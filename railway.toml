[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c \"gunicorn tracker_system.wsgi:application --bind 0.0.0.0:${PORT:-8000}\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
